//import { expect } from 'chai';
//import PdfHandler from '../utils/pdfHandler';

/*describe('PDF Handler - check file path', () => {
  it('Should display pdf does not exist.', async () => {
    const response = await PdfHandler.split("C:\\htdocs\\test.pdf", "")
    expect(response).to.contain('does not exist')
  })

  it('Should be greater than 0.', async () => {
    const mainDir = "C:\\eStream\\officialreceipts\\";
    const splitDir = "C:\\eStream\\split\\officialreceipts";
    const response = await PdfHandler.split(`${mainDir}E-Official Receipt.pdf`, `${splitDir}`);
    expect(response).to.greaterThan(0);
  })
})*/

/*describe('PDF Handler - parse PDF', async () => {
  const pdf = "C:\\eStream\\split\\officialreceipts\\output_0.pdf";
  const response = await PdfHandler.parse(pdf);
  expect(response).to.be.have.key('Pages');
});*/